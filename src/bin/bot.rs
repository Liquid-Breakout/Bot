use std::env;
use liquid_breakout_backend::id_converter::IDConverter;
//serenity things
use serenity::async_trait;
use serenity::model::channel::Message;
use serenity::model::gateway::Ready;
use serenity::prelude::*;

struct Handler;

fn test_tonumber() -> Result

fn main (){



    return (0)
}